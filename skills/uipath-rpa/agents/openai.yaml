interface:
  display_name: "UiPath RPA"
  short_description: "Preserve UiPath contracts and proof layers"
  default_prompt: "Use $uipath-rpa to establish the live project contract, change only the authorized slice, and report each required validation layer without overclaiming."
