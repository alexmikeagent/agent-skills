interface:
  display_name: "Tasteful HTML Artifacts"
  short_description: "Create contextual, source-faithful HTML artifacts"
  default_prompt: "Use $tasteful-html-artifacts to transform this material into a context-specific, source-faithful HTML reading interface."
